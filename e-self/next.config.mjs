/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // ❌ Remove `appDir: true` (It's already the default in Next.js 15)
  },
  reactStrictMode: true,
};

export default nextConfig;
